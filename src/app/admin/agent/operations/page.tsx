import { Card } from "@/components/ui";

export default function AdminAgentOperationsPage() {
  return (
    <div className="max-w-3xl">
      <Card className="p-6 text-slate-600 text-sm">
        运营素材 / 活动 / 转化跟踪。与仪表盘「数据时段」筛选共享维度。
      </Card>
    </div>
  );
}
